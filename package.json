{
  "name": "planar",
  "description": "Graph-powered plan & diff review for Claude Code",
  "author": "steinivan",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/steinivan/planar"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/ui && bun run build && cd ../.. && bun build src/index.ts --compile --outfile planar",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "typecheck:ui": "cd packages/ui && svelte-check --tsconfig ./tsconfig.json",
    "check": "bun run format:check && bun run typecheck && bun run typecheck:ui",
    "test": "bun test tests/unit tests/integration",
    "test:e2e": "bunx playwright test",
    "test:all": "bun test tests/unit tests/integration && bunx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/bun": "^1.3.14",
    "prettier": "^3.8.1",
    "prettier-plugin-svelte": "^3.5.0",
    "svelte-check": "^4.6.0"
  }
}
